<div class='container'>
  <div class='row'>
    <div class='col-md-3 shadow'>
        <%= render "admin/searches/form",  search_params: @search_params %>
    </div>
    <div class='col-md-9 shadow'>
      <h2>管理煙一覧</h2>
      <%= link_to '新しい順', admin_posts_path(search: @search_params.merge(sort: "latest")) %>
      |<%= link_to '古い順', admin_posts_path(search: @search_params.merge(sort: "old")) %>
      |<%= link_to '評価の高い順', admin_posts_path(search: @search_params.merge(sort: "star_count")) %>
      <%= render "admin/posts/index", posts: @posts %>
    </div>
  </div>
</div>